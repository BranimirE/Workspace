#include <cstdio>
#include <vector>
#include <cstring>
#include <iostream>
#include <vector>
using namespace std;

#define MAX 100010
typedef long long int Number;
Number v[MAX] = {0};
int n, k;
vector<Number> solve(vector<Number> &vec){//minimo tamaño

}
int main(){
	int t;

	//scanf("%d", &t);
	//while(t--){
		//scanf("%d %d", &n, &k);
		cin >> n;
		v[0] = 0;
		for(int i = 1; i <= n; i++)
			scanf("%lld", &v[i]);
		solve(3);
	//}

	return 0;
}
