#include <bits/stdc++.h>

using namespace std;

int main(){
	/*int a, b, c, cups, medals, n;
	cin >> a >> b >> c;
	cups = a + b + c;
	cin >> a >> b >> c;
	medals = a + b + c;
	cin >> n;
	n -= (cups / 5);
	if(cups % 5)
		n--;
	n -= (medals / 10);
	if(medals % 10)
		n--;
	printf((n >= 0)?"YES\n":"NO\n");*/
	//cout << (.0 / 5.0) << endl;
	cout << ceil(51.0/5.0) << endl;
	cout << ceil(5.3) << endl;
	cout << 51.0 / 5.0 << endl;
	return 0;
}
